<div class="container">
  <h3 class="title is-3"><%= @author.name %></h3>
  <article>
    <div class="message is-dark">
      <p>Books from this author:</p>
    </div>
    <div class="message-body">
      <%= @author.books.map(&:title).join(", ")  %>
    </div>
  </article>
</div>