<h3 class="title is-3"><%= @publisher.name %></h3>
<article>
  <div class="message is-dark">
    <p>Books:</p>
  </div>
  <div class="message-body">
    <%= @publisher.books.map(&:title).join(", ")  %>
  </div>
</article>